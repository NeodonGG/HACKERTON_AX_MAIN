- **날짜 형식 통일**: ISO(`2026-05-02`) / 슬래시(`2026/05/06`) / 한글(`5월 4일`) 3종 혼용을 `YYYY-MM-DD`로 통일. 한글 표기는 연도 정보가 없어 데이터 전체가 몰려 있는 2026년으로 보완.
- **중복 제거**: id 001과 031이 완전 동일 → 031 제거, 45건 → 44건 기준으로 집계.
- **채널 결측 처리**: id 032, 035는 임의 추정하지 않고 결측(미상) 상태 유지.
- **경계 케이스 분류 기준**: "불만 vs 기능 요청"은 문제의 대상이 **이미 존재하는 기능의 결함/제한**인지, **아직 없는 기능의 신설 요청**인지로 구분. 이 기준에 따라 id 043(보고서 커스터마이징 제한 + 개선요청 혼재)은 요청 문장이 있음에도 "불만"으로 분류 — problem.md가 제시한 정답 힌트와 일치.
- **계산 신뢰성 문제(id 014, 033)**: 질문형 문장이지만 카본링크 산출물의 정확성에 대한 문제 제기가 핵심이므로 "불만"으로 분류(단순 정보 확인 질문과 구분).
- **파이프라인 검증**: 규칙 기반 분류기를 원본 44건에 실행해 수동 분류와 43/44건(97.7%) 일치 확인. 유일한 불일치(id 038)는 "기능이 없어서 불편"과 "기능이 있는데 불편"을 키워드만으로 구분할 수 없는 근본적 한계로 판단, 임의 보정하지 않고 투명하게 기록.
- **우선순위 가중치(비즈니스 임팩트 45%·규제 긴급성 25%·VoC 빈도 30%)**: PCA/CRITIC/Entropy 같은 데이터 기반 가중치 산출법은 기획안이 2건뿐이라 통계적으로 무의미하다고 판단해 기각. 대신 가중치 시나리오 4종으로 민감도 분석을 수행해 순위가 극단적 가중치(단일 기준 100%)에서만 뒤집힌다는 점을 확인.

(전체 판단 근거와 대안 비교는 `decisions.md` 참조)
